<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="@dimen/layout_height_l">

    <TextView
        android:id="@+id/wid_top_bar_tv_title"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_margin="@dimen/margin_s"
        android:ellipsize="marquee"
        android:gravity="center"
        android:marqueeRepeatLimit="marquee_forever"
        android:scrollHorizontally="true"
        android:textColor="#333"
        android:textSize="@dimen/text_size_s"
        app:layout_constraintEnd_toStartOf="@id/wid_top_bar_imb_right"
        app:layout_constraintStart_toEndOf="@id/wid_top_bar_imb_left" />

    <ImageButton
        android:id="@+id/wid_top_bar_imb_left"
        android:layout_width="@dimen/layout_width_s"
        android:layout_height="@dimen/layout_height_s"
        android:layout_margin="@dimen/margin_m"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageButton
        android:id="@+id/wid_top_bar_imb_right"
        android:layout_width="@dimen/layout_width_s"
        android:layout_height="@dimen/layout_height_s"
        android:layout_margin="@dimen/margin_m"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <View
        android:layout_width="match_parent"
        android:layout_height="1px"
        android:background="@color/gray_200"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>